package servent.handler.snapshot;

import app.AppConfig;
import app.snapshot_bitcake.SnapshotCollector;
import servent.handler.MessageHandler;
import servent.message.Message;
import servent.message.MessageType;
import servent.message.snapshot.LYTellMessage;

public class RejectHandler implements MessageHandler {

//    private Message clientMessage;
//
//    public RejectHandler(Message clientMessage) {
//        this.clientMessage = clientMessage;
//
//    }
    @Override
    public void run() {
//        if (clientMessage.getMessageType() == MessageType.REJECT) {
//            LYTellMessage lyTellMessage = (LYTellMessage)clientMessage;
//
//
//        } else {
//            AppConfig.timestampedErrorPrint("Tell amount handler got: " + clientMessage);
//        }
    }
}
